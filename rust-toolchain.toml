# Pins the toolchain used to build and check this repository. The MSRV that
# consumers must satisfy is `rust-version` in the workspace manifest, which is
# a separate and looser constraint.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
profile = "minimal"

# unwrap, expect, and panic are how a test reports failure. The workspace
# manifest denies them so they cannot reach a production path; these three
# exemptions keep that deny meaningful instead of pushing test authors to
# contort assertions or to blanket-allow the lint per module.
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-panic-in-tests = true
